# 🎯 Шпаргалка для защиты проекта BoomBet

## Краткое описание (30 секунд)

**BoomBet** - букмекерская платформа на микросервисной архитектуре.

**4 микросервиса:**

1. **API Gateway** - единая точка входа, JWT валидация
2. **Auth Service** - регистрация, логин, генерация токенов
3. **Core Service** - события, ставки, расчет выигрышей, транзакции
4. **Realtime Service** - генерация mock событий, имитация внешнего API

**Коммуникация:**

- HTTP/REST между Frontend ↔ Gateway ↔ Services
- Kafka для асинхронных событий (Realtime → Core)
- PostgreSQL как центральное хранилище

---

## 🔥 Ключевые преимущества (обязательно упомянуть!)

| Преимущество                       | Объяснение                                                                |
| ---------------------------------- | ------------------------------------------------------------------------- |
| **Независимое развертывание**      | Можно обновить Auth Service без остановки Core Service                    |
| **Горизонтальное масштабирование** | При росте нагрузки запускаем больше инстансов Core Service                |
| **Изоляция отказов**               | Если Realtime Service упадет, пользователи все равно могут ставить ставки |
| **Технологическое разнообразие**   | Можем использовать разные языки/фреймворки для разных сервисов            |
| **Параллельная разработка**        | Разные команды работают над разными сервисами                             |

---

## 📋 Ответы на топ-10 вопросов

### 1. Почему микросервисы, а не монолит?

**Короткий ответ:**
Букмекерская платформа имеет разные нагрузки на разные части. Во время важного матча нагрузка на Core Service (ставки) вырастает в разы, но Auth Service не нужно масштабировать. Микросервисы позволяют масштабировать только нужные части.

**Детальный ответ:**

- **Изоляция**: Проблема в генераторе событий не влияет на прием ставок
- **Масштабирование**: Можем запустить 5 инстансов Core Service и 1 Auth Service
- **Гибкость**: Легко добавить новый сервис (например, Notification Service)
- **Разработка**: Можно обновлять Auth без пересборки всего проекта

---

### 2. Зачем API Gateway, если можно напрямую к сервисам?

**Короткий ответ:**
Gateway - это "охранник" на входе. Проверяет JWT токен один раз, а не в каждом микросервисе.

**Детальный ответ:**

- **Безопасность**: JWT валидация в одном месте
- **Упрощение клиента**: Frontend знает только один URL (http://localhost:8080)
- **Маршрутизация**: `/api/auth/**` → Auth, `/api/v1/**` → Core
- **Load Balancing**: Может распределить запросы между несколькими инстансами
- **Rate Limiting**: Защита от DDoS

---

### 3. Почему Kafka, а не просто REST между сервисами?

**Короткий ответ:**
Realtime Service генерирует 30 событий в минуту. Если бы он вызывал REST API Core Service, при ошибке события потерялись бы. Kafka гарантирует доставку.

**Детальный ответ:**

- **Асинхронность**: Realtime не ждет ответа от Core
- **Надежность**: Kafka сохраняет события на диск, можно переобработать
- **Масштабируемость**: Можно добавить несколько Consumer'ов
- **Decoupling**: Сервисы не знают друг о друге напрямую
- **Event Sourcing**: История всех событий сохраняется

**Когда используем REST:**
Когда нужен синхронный ответ (регистрация, размещение ставки).

---

### 4. Как обеспечивается консистентность данных?

**Короткий ответ:**
Используем ACID транзакции PostgreSQL для критичных операций (ставки, выплаты).

**Детальный ответ:**

**Сильная консистентность (ACID):**

```java
@Transactional
public Bet placeBet(BetRequest request) {
    // 1. Списать баланс
    user.setBalance(user.getBalance() - stake);
    // 2. Создать ставку
    Bet bet = new Bet();
    // 3. Создать транзакцию
    Transaction tx = new Transaction();
    // Если любой шаг упадет - ROLLBACK
}
```

**Eventual Consistency:**

- Realtime Service отправляет событие в Kafka
- Core Service обрабатывает асинхронно (задержка ~секунды)
- Это нормально для обновления коэффициентов

---

### 5. Что если Core Service упадет?

**Короткий ответ:**
API Gateway вернет 503 Service Unavailable. Можно настроить Circuit Breaker для возврата cached данных.

**Детальный ответ:**

**Текущая реализация:**

- Docker автоматически перезапустит контейнер
- Kafka сохранит события, они обработаются после восстановления

**Production решения:**

- **Health Checks**: Kubernetes мониторит `/actuator/health`
- **Replicas**: 3 инстанса Core Service за Load Balancer
- **Circuit Breaker**: После 5 ошибок возвращаем cached данные
- **Graceful Shutdown**: Завершаем обработку текущих запросов

---

### 6. Как работает JWT аутентификация?

**Короткий ответ:**
JWT - это зашифрованная "пропуска", которую сервер выдает после логина. Frontend прикрепляет её к каждому запросу.

**Пошагово:**

```
1. Пользователь: email + password
   ↓
2. Auth Service: проверяет, генерирует JWT
   ↓
3. Frontend: сохраняет в localStorage
   ↓
4. Каждый запрос: Authorization: Bearer <JWT>
   ↓
5. API Gateway: проверяет подпись JWT
   ↓
6. Если валидный → пропускает
   Если невалидный → 401 Unauthorized
```

**Структура JWT:**

```
eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJ1c2VyQGV4YW1wbGUuY29tIn0.signature
│                     │                                    │
Header                Payload (email, exp)                Signature (проверка)
```

**Срок действия:** 10 часов

---

### 7. Как работает расчет ставок?

**Короткий ответ:**
Каждые 30 секунд BetSettlementScheduler проверяет завершенные события и рассчитывает ставки.

**Детальный ответ:**

```java
@Scheduled(fixedRate = 30000) // Каждые 30 секунд
public void settleBets() {
    // 1. Найти события со status = "finished"
    List<Event> events = eventRepository.findByStatus("finished");

    for (Event event : events) {
        // 2. Найти pending ставки на это событие
        List<Bet> bets = betRepository.findPendingBetsByEventId(event.getId());

        // 3. Определить выигрышные outcomes по result (например, "2:1")
        List<Long> winningOutcomes = determineWinners(event);

        for (Bet bet : bets) {
            // 4. Проверить все selections в ставке
            boolean won = bet.getSelections().stream()
                .allMatch(sel -> winningOutcomes.contains(sel.getOutcomeId()));

            if (won) {
                // 5. Зачислить выигрыш
                bet.setStatus("WON");
                user.setBalance(user.getBalance() + bet.getPotentialPayout());
                // 6. Создать транзакцию
                Transaction tx = new Transaction(user, bet.getPayout(), "WIN");
            } else {
                bet.setStatus("LOST");
            }
        }

        // 7. Обновить статус события
        event.setStatus("settled");
    }
}
```

---

### 8. Как добавить новый микросервис?

**Пример: Notification Service для email уведомлений**

**Шаги:**

1. **Создать Spring Boot приложение**

   ```bash
   spring init -d=web,kafka notification-service
   ```

2. **Добавить в docker-compose.yml**

   ```yaml
   notification-service:
     build: ./notification-service
     ports:
       - '8084:8084'
     depends_on:
       - kafka
     networks:
       - boombet-network
   ```

3. **Добавить Kafka Consumer**

   ```java
   @KafkaListener(topics = "notifications")
   public void handleNotification(NotificationDTO dto) {
       emailService.send(dto.getEmail(), dto.getMessage());
   }
   ```

4. **Настроить маршрут в API Gateway** (опционально)
   ```java
   .route("notifications", r -> r.path("/api/notifications/**")
       .uri("lb://notification-service"))
   ```

**Плюсы:** Не нужно трогать остальные сервисы!

---

### 9. Как масштабировать систему?

**Горизонтальное масштабирование (добавить серверов):**

**Вариант 1: Docker Compose (для тестов)**

```bash
docker-compose up --scale core-service=3
```

Запустит 3 инстанса Core Service. Gateway автоматически распределит нагрузку.

**Вариант 2: Kubernetes (production)**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: core-service
spec:
  replicas: 5 # 5 инстансов
  selector:
    matchLabels:
      app: core-service
```

**Вертикальное масштабирование (больше ресурсов):**

```yaml
core-service:
  deploy:
    resources:
      limits:
        cpus: '2.0'
        memory: 4G
```

**Когда что масштабировать:**

- **Core Service** - при высокой нагрузке на ставки
- **Auth Service** - при большом количестве регистраций
- **PostgreSQL** - Read Replicas для чтения, Master для записи

---

### 10. Какие технологии и почему?

| Технология               | Почему выбрали                                           |
| ------------------------ | -------------------------------------------------------- |
| **Spring Boot**          | Стандарт для enterprise Java, богатая экосистема         |
| **PostgreSQL**           | ACID транзакции для финансовых операций, надежность      |
| **Kafka**                | Высокая пропускная способность, гарантированная доставка |
| **Spring Cloud Gateway** | Интеграция с Spring экосистемой, реактивный подход       |
| **JWT**                  | Stateless аутентификация, легко масштабируется           |
| **Docker**               | Изоляция сервисов, легкое развертывание                  |
| **React**                | Компонентный подход, большая экосистема, Virtual DOM     |
| **TypeScript**           | Type safety, меньше ошибок в runtime                     |

---

## 🚀 Демонстрация работы системы

### Сценарий для показа:

**1. Запуск системы**

```bash
cd boombet-backend
docker-compose up -d
cd ../boombet-frontend
npm run dev
```

**2. Регистрация пользователя**

- Открыть http://localhost:3000/register
- Зарегистрироваться: test@example.com / password123
- Показать JWT токен в Network DevTools

**3. Просмотр live событий**

- Перейти на /live
- Показать список событий и коэффициентов
- Объяснить: "Realtime Service генерирует эти события каждые 2 минуты"

**4. Размещение ставки**

- Выбрать исход (например, Team A to win, odds 2.50)
- Ввести ставку 50 рублей
- Показать расчет: potential payout = 50 \* 2.50 = 125
- Нажать "Поставить"
- Показать в Network: POST /api/v1/bets → 201 Created

**5. Завершение события (админка)**

- Открыть /admin
- Выбрать событие, нажать "Finish with 2:1"
- Объяснить: "Теперь BetSettlementScheduler в течение 30 секунд обработает ставку"

**6. Просмотр результата**

- Перейти на /bets
- Обновить через 30 секунд
- Показать статус "WON" и зачисление выигрыша

**7. Показать логи**

```bash
docker logs boombet-core-service
```

Найти:

```
INFO: Looking for pending bets for event 1719
INFO: Found 1 bets from repository
INFO: Bet 7 WON, crediting 125.00 to user
```

---

## 📊 Метрики проекта (для впечатления)

- **Микросервисов:** 4
- **Endpoint'ов:** 15+
- **Таблиц в БД:** 8
- **Kafka Topics:** 2
- **Scheduled задач:** 3
- **Docker контейнеров:** 7
- **Строк кода:** ~3000 (backend) + ~2000 (frontend)

---

## 🎤 Как начать презентацию

**Вариант 1 (технический):**

> "BoomBet - это букмекерская платформа, построенная на микросервисной архитектуре с использованием Spring Boot, React и Apache Kafka. Система состоит из 4 независимых микросервисов, которые коммуницируют через REST API и event streaming."

**Вариант 2 (бизнес):**

> "Мы создали платформу для приема ставок на спортивные события. Главная особенность - микросервисная архитектура, которая позволяет масштабировать систему во время важных матчей, когда нагрузка вырастает в десятки раз."

**Вариант 3 (проблема-решение):**

> "Букмекерские компании сталкиваются с огромными нагрузками во время финалов чемпионата. Монолитная архитектура не справляется. Мы разделили систему на микросервисы, чтобы масштабировать только ту часть, которая перегружена."

---

## ⚡ Быстрые факты (для уверенности)

- **JWT срок:** 10 часов
- **Scheduler интервал:** 30 секунд (settlement), 30 секунд (status), 2 минуты (mock data)
- **Kafka топик:** events-updates
- **Порт Gateway:** 8080
- **База данных:** PostgreSQL 15
- **ORM:** Hibernate/JPA
- **Connection Pool:** HikariCP

---

## 🔧 Возможные улучшения (если спросят)

### Краткосрочные:

- [ ] WebSocket для live обновлений коэффициентов
- [ ] Redis cache для списка событий
- [ ] Admin панель для управления событиями

### Среднесрочные:

- [ ] Notification Service для email/push уведомлений
- [ ] Analytics Service для статистики и отчетов
- [ ] Circuit Breaker pattern (Resilience4j)

### Долгосрочные:

- [ ] Kubernetes для оркестрации
- [ ] Интеграция с реальным API (Sportradar)
- [ ] GraphQL вместо REST для гибких запросов
- [ ] Saga pattern для распределенных транзакций

---

## 🎯 Главное - уверенность!

**Помни:**

1. Ты создал **работающую** систему
2. Она **масштабируемая** (можно добавить инстансы)
3. Она **расширяемая** (легко добавить новые сервисы)
4. Она использует **современные технологии** (Spring Boot 3, React 18, Kafka)
5. Она **production-ready** (Docker, ACID транзакции, JWT)

**Если не знаешь ответ:**

- "Это интересный вопрос, в production я бы использовал [название паттерна]"
- "Сейчас это реализовано просто, но можно улучшить через [технология]"
- "Я рассматривал этот вариант, но выбрал текущий из-за [причина]"

---

## 📝 Чек-лист перед защитой

- [ ] Запустил `docker-compose up` - все сервисы работают
- [ ] Проверил логи - нет ошибок
- [ ] Frontend открывается на :3000
- [ ] Могу зарегистрироваться и войти
- [ ] Вижу live события
- [ ] Могу поставить ставку
- [ ] Знаю, как показать схему архитектуры
- [ ] Повторил ответы на топ-10 вопросов
- [ ] Подготовил демонстрацию работы
- [ ] Уверен в себе! 💪

---

**Удачи на защите! 🚀**
