# Шпаргалка для защиты проекта BoomBet

## Быстрые ответы на частые вопросы

### 1. Что это за проект?

**Ответ:** BoomBet - это платформа для онлайн-ставок на спортивные события, построенная на микросервисной архитектуре. Система позволяет пользователям просматривать live и предстоящие спортивные события, размещать ставки, управлять своим балансом и получать real-time обновления коэффициентов.

---

### 2. Почему микросервисы, а не монолит?

**Ответ:**

1. **Масштабируемость** - Core Service (обработка ставок) требует больше ресурсов чем Auth Service
2. **Изоляция сбоев** - если упадет Realtime Service, пользователи все равно смогут просматривать события и делать ставки
3. **Независимая разработка** - разные команды могут работать параллельно
4. **Гибкость технологий** - можем использовать разные БД или языки для разных сервисов
5. **Независимые релизы** - можно обновить Auth Service без перезапуска всей системы

**Недостатки признаны:**

- Сложнее debugging
- Network latency
- Operational overhead
- Но для нашей задачи (real-time betting) плюсы перевешивают

---

### 3. Какие сервисы у вас есть?

**Ответ:**

#### **API Gateway (Port 8080)**

- Единая точка входа
- Маршрутизация запросов
- Валидация JWT токенов
- CORS настройка

#### **Auth Service (Port 8081)**

- Регистрация и аутентификация
- Генерация JWT токенов
- Хранение пользователей
- BCrypt хеширование паролей

#### **Core Service (Port 8082)**

- Управление событиями (events, markets, outcomes)
- Размещение ставок (bets, bet_items)
- Управление балансом (transactions)
- Kafka consumer для real-time обновлений

#### **Realtime Service (Port 8083)**

- Генерация mock данных (scheduler каждые 15 секунд)
- Имитация внешних API букмекеров
- Kafka producer
- В будущем - WebSocket для frontend

---

### 4. Почему используете API Gateway?

**Ответ:**

1. **Security** - централизованная проверка JWT вместо дублирования в каждом сервисе
2. **Упрощение клиента** - frontend знает только один адрес (localhost:8080)
3. **Routing** - легко добавлять новые сервисы без изменения клиента
4. **Cross-cutting concerns** - logging, monitoring, rate limiting в одном месте
5. **Version management** - можем поддерживать /api/v1 и /api/v2 одновременно

**Альтернативы:**

- Backend for Frontend (BFF) - если нужны разные API для web/mobile
- Service Mesh (Istio) - для более сложных сценариев
- Но для нашего масштаба Spring Cloud Gateway оптимален

---

### 5. Объясните Kafka в вашей системе

**Ответ:**

**Producer:** Realtime Service генерирует события (новые матчи, обновления коэффициентов) и публикует в topic `events-updates`

**Consumer:** Core Service читает из топика и обновляет базу данных

**Почему Kafka, а не REST?**

| Требование      | REST               | Kafka           |
| --------------- | ------------------ | --------------- |
| Синхронность    | Realtime ждет Core | Асинхронно      |
| Надежность      | Потеря при сбое    | Персистентность |
| Масштабирование | 1:1                | 1:N consumers   |
| Coupling        | Tight              | Loose           |
| Replay          | Нет                | Да              |

**Пример:** Если Core Service упадет на 5 минут, при восстановлении он прочитает все пропущенные сообщения из Kafka и обновит БД.

**Альтернативы:**

- RabbitMQ - проще, но меньше throughput
- Redis Pub/Sub - быстрее, но нет персистентности
- REST + Queue - сложнее

---

### 6. Как работает аутентификация?

**Ответ:**

**Шаг 1: Login**

```
User → Frontend → API Gateway → Auth Service
                                    ↓
                              проверка пароля (BCrypt)
                                    ↓
                              генерация JWT (secret + expiration)
                                    ↓
Frontend ← API Gateway ← { token: "eyJ...", user: {...} }
                                    ↓
                            localStorage.setItem('token')
```

**Шаг 2: Защищенный запрос**

```
Frontend → API Gateway (с заголовком Authorization: Bearer eyJ...)
              ↓
         валидация JWT:
         - проверка подписи (secret key)
         - проверка expiration
         - извлечение email
              ↓
         добавление заголовка X-Authenticated-User-Email
              ↓
         Core Service → использует email для идентификации пользователя
```

**Почему JWT?**

- Stateless - не нужно хранить сессии на сервере
- Масштабируемость - любой инстанс может проверить токен
- Self-contained - содержит user info (claims)
- Standard - RFC 7519

**Security:**

- Secret key хранится в environment variables
- Токены expire (configurable)
- HTTPS в production (защита от MITM)

---

### 7. Как храните данные?

**Ответ:**

**Одна PostgreSQL база, но логическое разделение:**

```
Auth Service → users table
Core Service → events, markets, outcomes, bets, bet_items, transactions, sports
```

**Почему одна БД, а не Database per Service?**

Признание: Это компромисс для упрощения на стадии разработки.

**Идеально:** Каждому сервису свою БД
**Реальность:** Shared database с четким разделением таблиц

**Плюсы текущего подхода:**

- Проще настройка (один Docker container)
- JOIN'ы между таблицами (если нужно)
- ACID транзакции

**Минусы:**

- Нет полной изоляции
- Нельзя выбрать разные типы БД для сервисов

**В production бы сделал:**

```
Auth Service → отдельная PostgreSQL (user data)
Core Service → PostgreSQL (transactional data)
Events Service → MongoDB (flexible schema для разных видов спорта)
Cache → Redis (hot data - active events, odds)
```

---

### 8. Где самое узкое место (bottleneck)?

**Ответ:**

**Текущие bottlenecks:**

1. **Database** - все запросы к одной PostgreSQL

   - **Решение:** Read replicas, Connection pooling (HikariCP уже есть), Индексы

2. **Core Service** - обрабатывает и ставки, и события

   - **Решение:** Горизонтальное масштабирование (несколько инстансов)

3. **Kafka** - одна партиция в топике
   - **Решение:** Партиционирование по sport_id или event_id

**Как бы оптимизировал:**

```
Frontend
    ↓
API Gateway → Load Balancer
    ↓              ↓              ↓
Core-1      Core-2      Core-3    (горизонтальное масштабирование)
    ↓              ↓              ↓
PostgreSQL Master (Write) + Read Replicas (Read)
    ↓
Redis Cache (hot data - active events, odds)
```

---

### 9. Как тестируете систему?

**Ответ:**

**Unit Tests:**

```java
@Test
void testPlaceBet_Success() {
    // Тестирование логики размещения ставки
}
```

**Integration Tests:**

```java
@SpringBootTest
@AutoConfigureTestDatabase
class BetServiceIntegrationTest {
    // Тест с реальной БД (H2 in-memory)
}
```

**E2E Tests (Docker Compose):**

```bash
docker-compose up -d
curl http://localhost:8080/api/v1/events/live
# Проверка всей цепочки: Gateway → Core → DB
```

**Load Testing (планируется):**

```
JMeter / Gatling
- 1000 concurrent users
- 10000 requests/sec
- Latency < 200ms
```

---

### 10. Что бы улучшили?

**Ответ - честный список TODO:**

**1. Observability**

- Prometheus + Grafana для метрик
- ELK Stack для логов
- Zipkin для distributed tracing
- Alert manager

**2. Service Discovery**

- Consul или Eureka
- Убрать хардкод адресов из конфигов

**3. Circuit Breaker**

- Resilience4j
- Защита от cascading failures

**4. Cache Layer**

- Redis для hot data
- Снизить нагрузку на DB

**5. Security**

- Rate limiting
- IP whitelisting
- DDoS protection (CloudFlare)
- HTTPS certificates

**6. Real API Integration**

- Заменить mock на реальные API букмекеров
- Aggregation из нескольких источников

**7. WebSocket**

- Bi-directional communication
- Push notifications для изменения odds
- Лучше чем polling

**8. CI/CD**

- GitHub Actions
- Автоматические тесты
- Deployment в Kubernetes

---

### 11. Сколько пользователей выдержит?

**Ответ - честная оценка:**

**Текущая конфигурация (1 инстанс каждого сервиса):**

- ~100 concurrent users
- ~1000 requests/second
- Limited by single PostgreSQL instance

**С оптимизацией:**

```
API Gateway (2 instances) → 10K req/s
Core Service (5 instances) → 5K req/s
PostgreSQL (Master + 3 Read Replicas) → 10K req/s
Redis Cache (hit rate 80%) → 50K req/s
Kafka (3 partitions) → 100K messages/s
```

**Bottleneck после оптимизации:** PostgreSQL Write operations (размещение ставок)

**Решение:** Sharding по user_id или Eventual Consistency через Saga pattern

---

### 12. Почему Spring Boot?

**Ответ:**

**Преимущества:**

1. **Production-ready** - actuator, metrics, health checks из коробки
2. **Экосистема** - Spring Cloud, Spring Security, Spring Data
3. **Convention over configuration** - быстрая разработка
4. **Community** - огромное сообщество, документация

**Альтернативы рассматривали:**

- Node.js (Express) - проще для real-time, но слабее для транзакций
- Go - быстрее, но меньше библиотек для enterprise
- Python (FastAPI) - проще, но медленнее

**Для нашего проекта Spring Boot оптимален:**

- Финансовые транзакции (ACID)
- Enterprise patterns (DI, AOP)
- Зрелость для production

---

### 13. Как деплоите?

**Ответ:**

**Локально (dev):**

```bash
docker-compose up --build
```

**Production (планируется):**

```
1. Build
   - Maven: mvn clean package
   - Docker: docker build -t service:v1.0

2. Push to Registry
   - docker push registry.com/service:v1.0

3. Deploy to Kubernetes
   - kubectl apply -f deployment.yaml
   - Helm charts для управления конфигом

4. Ingress
   - Nginx Ingress Controller
   - SSL/TLS termination

5. Monitoring
   - Prometheus scraping metrics
   - Grafana dashboards
```

**Rollback strategy:**

```
kubectl rollout undo deployment/core-service
```

---

### 14. Стоимость инфраструктуры?

**Ответ - примерный расчет для production:**

**AWS (us-east-1):**

```
EKS Cluster (3 nodes, t3.medium)     $200/month
RDS PostgreSQL (db.t3.large)         $150/month
ElastiCache Redis (cache.t3.micro)   $50/month
MSK Kafka (3 brokers, kafka.t3.small) $250/month
ALB (Load Balancer)                  $25/month
CloudFront CDN                       $50/month
S3 (logs, backups)                   $20/month
CloudWatch (monitoring)              $30/month

TOTAL: ~$775/month (~60000 RUB)
```

**Оптимизация для студенческого бюджета:**

- Digital Ocean Kubernetes: $120/month
- Managed PostgreSQL: $60/month
- Self-hosted Kafka: $0 (в Kubernetes)
- Cloudflare (Free tier): $0

**Total optimized:** ~$180/month (~14000 RUB)

---

### 15. Сколько времени заняла разработка?

**Ответ - честная оценка:**

```
Архитектура и дизайн:         2 дня
Backend (4 сервиса):          5 дней
Frontend (React):             3 дня
Integration и debugging:      2 дня
Docker и инфраструктура:      1 день
Тестирование:                1 день
Документация:                1 день

TOTAL: ~15 дней (3 недели)
```

**Распределение времени:**

- 40% - Backend development
- 25% - Frontend development
- 20% - Integration & debugging
- 15% - Infrastructure & docs

---

## Полезные команды для демонстрации

```bash
# Запуск системы
cd boombet-backend
docker-compose up -d

# Проверка статуса
docker-compose ps

# Логи конкретного сервиса
docker logs -f core-service

# Проверка БД
docker exec -it postgres psql -U betting_app_user -d BoomBet
\dt  # список таблиц
SELECT COUNT(*) FROM events;
SELECT COUNT(*) FROM bets;

# Kafka topics
docker exec -it kafka kafka-topics --list --bootstrap-server localhost:9092

# API Gateway health
curl http://localhost:8080/actuator/health

# Тестовый запрос событий
curl http://localhost:8080/api/v1/events/live

# Frontend
cd ../boombet-frontend
npm run dev
```

---

## Типичные вопросы от комиссии

### "А почему не использовали X?"

**Ответ:** Рассмотрели X, но выбрали Y по причине Z. Например, рассмотрели MongoDB для событий (flexible schema), но выбрали PostgreSQL для консистентности и ACID транзакций при ставках.

### "А как масштабируется?"

**Ответ:** [См. вопрос 11] Горизонтальное масштабирование через Kubernetes, база на Read Replicas, Kafka партиционирование.

### "А безопасность?"

**Ответ:** JWT для аутентификации, BCrypt для паролей, API Gateway для централизованной security, CORS для frontend, HTTPS в production.

### "А что если сервис упадет?"

**Ответ:** Изоляция сбоев - один упавший сервис не роняет систему. Kafka персистентность для асинхронных операций. Health checks в Kubernetes для auto-restart.

### "Сколько это стоит?"

**Ответ:** [См. вопрос 14] ~$180-800/month в зависимости от оптимизации.

---

## Подготовка к демо

1. **Запусти систему заранее** (docker-compose up -d за 5 минут до защиты)
2. **Открой логи в терминале** (docker logs -f core-service)
3. **Открой frontend в браузере** (localhost:3000)
4. **Подготовь тестового пользователя** (зарегистрируй заранее)
5. **Открой диаграммы** (АРХИТЕКТУРА.md в VS Code с Mermaid preview)

---

## Главное правило защиты

**Будь честным:** Лучше признать недостаток и объяснить как его исправил бы, чем утверждать что все идеально.

**Примеры честности:**

- "Да, сейчас все в одной БД - это упрощение. В production бы разделил."
- "Пока mock данные, но архитектура позволяет легко подключить реальные API."
- "Monitoring недоделан, но я знаю как добавить Prometheus."

**Это показывает:**

- Понимание проблем
- Знание best practices
- Практический подход
- Готовность к критике

Удачи на защите! 🚀
