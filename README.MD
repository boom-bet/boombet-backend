# 🎰 BoomBet Backend

Микросервисная архитектура для платформы ставок на спортивные события.

## 📋 Описание проекта

BoomBet - это современная платформа для ставок на спорт, построенная на микросервисной архитектуре с использованием Spring Boot 3.5.6 и Java 21. Система обеспечивает управление пользователями, обработку ставок, real-time обновления событий и безопасную аутентификацию.

## 🏗️ Архитектура

Проект состоит из 4 микросервисов:

### 🌐 API Gateway (порт 8080)

- Единая точка входа для всех клиентских запросов
- Маршрутизация запросов к внутренним микросервисам
- Применение политик безопасности и валидация JWT токенов
- Балансировка нагрузки

### 🔐 Auth Service (порт 8081)

- Регистрация и аутентификация пользователей
- Генерация и валидация JWT токенов
- Управление сессиями пользователей
- Интеграция с PostgreSQL для хранения данных пользователей

### ⚡ Core Service (порт 8082)

- Центральная бизнес-логика приложения
- Управление спортивными событиями и матчами
- Обработка ставок и транзакций
- Управление профилями пользователей и балансом
- Интеграция с PostgreSQL, Redis и Kafka
- Публикация событий в Kafka для real-time обновлений

### 📡 Realtime Service (порт 8083)

- Real-time обновления событий через WebSocket
- Парсинг и агрегация данных о спортивных событиях
- Использование Selenium для веб-скрапинга
- Потребление событий из Kafka

## 🛠️ Технологический стек

### Backend

- **Java 21** - основной язык программирования
- **Spring Boot 3.5.6** - фреймворк для микросервисов
- **Spring Cloud** - инструменты для распределенных систем
- **Spring Security** - аутентификация и авторизация
- **Spring Data JPA** - работа с базой данных
- **JWT (jjwt 0.11.5)** - токены аутентификации

### Базы данных и брокеры сообщений

- **PostgreSQL 15** - основная реляционная БД
- **Redis** - кэширование данных
- **Apache Kafka** - обработка событий и асинхронное взаимодействие
- **Zookeeper** - координация Kafka

### Инструменты

- **Docker & Docker Compose** - контейнеризация
- **Maven** - управление зависимостями и сборка
- **Selenium 4.21** - веб-скрапинг
- **Jsoup 1.17** - парсинг HTML

## 🚀 Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Java 21 (для локальной разработки)
- Maven 3.6+ (для локальной разработки)

### Запуск через Docker Compose

1. Клонируйте репозиторий:

```bash
git clone <repository-url>
cd boombet-backend
```

2. Запустите все сервисы:

```bash
docker-compose up -d
```

3. Проверьте статус контейнеров:

```bash
docker-compose ps
```

Сервисы будут доступны по следующим адресам:

- API Gateway: http://localhost:8080
- Auth Service: http://localhost:8081
- Core Service: http://localhost:8082
- Realtime Service: http://localhost:8083
- PostgreSQL: localhost:5432
- Kafka: localhost:9092

### Остановка сервисов

```bash
docker-compose down
```

Для удаления volumes (данные БД):

```bash
docker-compose down -v
```

## 🔧 Локальная разработка

### Сборка отдельного сервиса

```bash
cd <service-name>
./mvnw clean install
```

### Запуск сервиса локально

```bash
./mvnw spring-boot:run
```

### Запуск тестов

```bash
./mvnw test
```

## 📊 База данных

### Конфигурация PostgreSQL

- **База данных:** BoomBet
- **Пользователь:** betting_app_user
- **Пароль:** 1234
- **Порт:** 5432

Данные сохраняются в Docker volume `postgres-data`.

## 📡 Kafka Topics

Сервисы взаимодействуют через Kafka для обработки событий в реальном времени:

- События ставок
- Обновления коэффициентов
- Уведомления пользователей

## 🔌 API Документация

### Основные эндпоинты

#### Аутентификация

- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/refresh` - Обновление токена

#### Профиль пользователя

- `GET /api/v1/users/profile` - Получить профиль
- `GET /api/v1/users/balance` - Получить баланс
- `GET /api/v1/users/transactions` - История транзакций

Подробную документацию API см. в [USER_PROFILE_API.md](core-service/USER_PROFILE_API.md)

## 🏗️ Структура проекта

```
boombet-backend/
├── api-gateway/          # Маршрутизация и балансировка
├── auth-service/         # Аутентификация и авторизация
├── core-service/         # Основная бизнес-логика
├── realtime-service/     # Real-time обновления
├── docker-compose.yml    # Оркестрация контейнеров
└── README.MD            # Документация проекта
```

## 🔐 Безопасность

- Все запросы проходят через API Gateway
- JWT токены для аутентификации
- Заголовок `X-Authenticated-User-Email` для идентификации пользователя
- Spring Security для защиты эндпоинтов

## 🤝 Взаимодействие сервисов

```
Client → API Gateway → Auth Service (аутентификация)
                   ↓
                Core Service (бизнес-логика)
                   ↓
                Kafka (события)
                   ↓
             Realtime Service (WebSocket обновления)
```

## 📝 Логирование и мониторинг

Логи доступны через Docker:

```bash
docker-compose logs -f <service-name>
```

## 🐛 Отладка

Для отладки конкретного сервиса:

```bash
docker-compose logs -f <service-name>
```

Для входа в контейнер:

```bash
docker exec -it <container-name> /bin/bash
```

## 📦 Сборка production образов

```bash
docker-compose build --no-cache
```

## 🧪 Тестирование

Каждый сервис содержит набор unit и integration тестов:

```bash
# Запуск всех тестов
./mvnw test

# Запуск с генерацией отчетов
./mvnw clean test jacoco:report
```

## 📄 Лицензия

Проект разработан в образовательных целях.

## 👥 Команда разработки

Проект разработан как часть учебного курса.
